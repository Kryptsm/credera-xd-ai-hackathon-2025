// Storybook story for Pattern
import React from 'react';
import { Pattern } from './Pattern';

export default {
  title: 'Components/Pattern',
  component: Pattern,
};

export const Default = () => <Pattern />;
