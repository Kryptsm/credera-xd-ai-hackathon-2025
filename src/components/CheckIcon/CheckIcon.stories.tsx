// Storybook story for CheckIcon
import React from 'react';
import { CheckIcon } from './CheckIcon';

export default {
  title: 'Components/CheckIcon',
  component: CheckIcon,
};

export const Default = () => <CheckIcon />;
