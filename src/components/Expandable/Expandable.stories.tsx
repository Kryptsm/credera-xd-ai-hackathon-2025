// Storybook story for Expandable
import React from 'react';
import { Expandable } from './Expandable';

export default {
  title: 'Components/Expandable',
  component: Expandable,
};

export const Default = () => <Expandable />;
