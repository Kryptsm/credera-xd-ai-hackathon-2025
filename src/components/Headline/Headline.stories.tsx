// Storybook story for Headline
import React from 'react';
import { Headline } from './Headline';

export default {
  title: 'Components/Headline',
  component: Headline,
};

export const Default = () => <Headline />;
